import axios from "axios";export default async function handler(req,res){if(req.method!=="POST")return res.status(405).json({error:"Method not allowed"});try{const{to,message}=req.body;if(!to||!message)return res.status(400).json({error:"Missing to/message"});const url=`https://graph.facebook.com/v20.0/${process.env.WA_PHONE_ID}/messages`;const payload={messaging_product:"whatsapp",to,type:"text",text:{body:message}};const r=await axios.post(url,payload,{headers:{Authorization:`Bearer ${process.env.WA_TOKEN}`,"Content-Type":"application/json"}});return res.status(200).json({success:true,data:r.data});}catch(e){return res.status(500).json({error:e.message});}}